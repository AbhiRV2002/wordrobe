import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.wordrobe.abhishek',
  appName: 'wordrobe',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
